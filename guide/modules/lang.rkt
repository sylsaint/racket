#lang racket

; The body of a #lang shorthand has no specific syntax, because the syntax is determined by the language name that follows #lang
; #lang raket decl ... represents the same meaning of (module mod_id racket decl ...)
